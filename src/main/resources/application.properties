spring.application.name=appointment-booking-system

# ===============================
# DATABASE CONFIGURATION (POSTGRES / SUPABASE)
# ===============================
spring.datasource.url=${DB_URL:jdbc:postgresql://db.sbejcmjjzkmxcptavfht.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Arun@9640568586}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# SERVER CONFIG (RAILWAY PORT BINDING)
# ===============================
server.port=${PORT:8080}

# ===============================
# JWT SECURITY CONFIGURATION
# ===============================
jwt.secret=${JWT_SECRET:HealthConnectSuperSecretProductionKey2026With256BitsMinimumLength}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ===============================
# EMAIL CONFIGURATION (GMAIL SMTP)
# ===============================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:arunkumarpusala7@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xspmjhffunrffhcr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# RAZORPAY CONFIGURATION
# ===============================
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TKm94gn5yKCNWX}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:gcO5LCvZUmY7UTNkB9ONqxjj}